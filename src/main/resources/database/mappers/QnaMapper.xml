<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.iu.home.board.qna.QnaMapper">
	<!-- id명은 method명과 동일하게 -->
	<select id="getList" resultType="com.iu.home.board.qna.QnaVO">
		SELECT * FROM QNA q WHERE NUM BETWEEN #{startRow} AND #{lastRow}
	</select>
	
	<insert id="setAdd" parameterType="QnaVO">
		INSERT INTO QNA (NUM, WRITER, TITLE, CONTENTS, REGDATE)
		VALUES (0, #{writer} , #{title}, #{contents}, now())
	</insert>
	
	<select id="getCount" resultType="Long" parameterType="Pager">
		SELECT COUNT(NUM) FROM QNA
	</select>
</mapper>    